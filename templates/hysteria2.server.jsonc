{
    "listen": "{{LISTEN}}",
    "port": {{PORT}},
    "protocol": "hysteria",
    "tag": "{{TAG}}",
    "settings": {
        "version": 2
    },
    "streamSettings": {
        "network": "hysteria",
        "security": "tls",
        "tlsSettings": {
            "alpn": ["h3"],
            "certificates": [{
                "usage": "encipherment",
                "certificateFile": "{{CERT_FILE}}",
                "keyFile": "{{KEY_FILE}}"
            }]
        },
        "hysteriaSettings": {
            "version": 2,
            "auth": "{{AUTH}}"
        },
        "finalmask": {
            "quicParams": {
                "congestion": "{{CONGESTION}}"{{BRUTAL_PARAMS_BLOCK}}{{UDPHOP_BLOCK}}
            }
        }
    },
    "sniffing": {
        "enabled": true,
        "destOverride": ["http", "tls", "quic"],
        "routeOnly": true
    }
}
