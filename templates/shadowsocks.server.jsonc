{
    "listen": "{{LISTEN}}",
    "port": {{PORT}},
    "protocol": "shadowsocks",
    "tag": "{{TAG}}",
    "settings": {
        "method": "{{METHOD}}",
        "password": "{{PASSWORD}}",
        "network": "tcp,udp"
    },
    "sniffing": {
        "enabled": true,
        "destOverride": ["http", "tls", "quic"],
        "routeOnly": true
    }
}
