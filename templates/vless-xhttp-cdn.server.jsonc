{
    "listen": "{{LISTEN}}",
    "port": {{PORT}},
    "protocol": "vless",
    "tag": "{{TAG}}",
    "settings": {
        "clients": [
            {
                "id": "{{UUID}}"
            }
        ],
        "decryption": "{{DECRYPTION}}"
    },
    "streamSettings": {
        "network": "xhttp",
        "xhttpSettings": {
            "mode": "auto",
            "path": "{{PATH}}",
            "host": "{{HOST}}"
        }
    },
    "sniffing": {
        "enabled": true,
        "destOverride": ["http", "tls", "quic"],
        "routeOnly": true
    }
}
